// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))q// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))q// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))q// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))X// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))P// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))X// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))P// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))G// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))G// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))|// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))|// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))—// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))!// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))J// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))|// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))|// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))q// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))|// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))|// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))7// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))×// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))×// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))~// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))×// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))×// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))~// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))—// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))P// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))P// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))—// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))—// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))—// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))z// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))q// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))z// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))P// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))q// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))z// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))q// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))z// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))—// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))%// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))%// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))P// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))~// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))!// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))P// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))Y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))+// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))*// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))E// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))—// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))z// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))8// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))<// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))3// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))B// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))W// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume));// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))A// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))C// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))$// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))`// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))&// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))?// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))/// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))M// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))6// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))=// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))>// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))T// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))S// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))I// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))N// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))-// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))[// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))]// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))9// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))1// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))U// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))w// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))4// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))V// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))v// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))R// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))F// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))2// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))k// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))y// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))p// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))x// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))b// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))h// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))t// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))u// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))5// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))j// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))(// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)){// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))r// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)).// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))m// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))s// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))g// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))a// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))l// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))H// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))O// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))L// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))D// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))'// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)),// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))o// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))f// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))i// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))d// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))n// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))c// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))e// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)):// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))0// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)))// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume)) // Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))}// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))
// Use Delta Exchange for OHLCV (Binance geo-blocks Vercel IPs)
    const DELTA_SYMS = {BTC:'BTCUSD',ETH:'ETHUSD',SOL:'SOLUSD',BNB:'BNBUSD',XRP:'XRPUSD',DOGE:'DOGEUSD',ADA:'ADAUSD'}
    const deltaSym = DELTA_SYMS[symbol] || `${symbol}USD`
    const nowTs = Math.floor(Date.now()/1000)
    const startTs = nowTs - (200 * 900)

    const r = await fetch(`https://api.india.delta.exchange/v2/history/candles?symbol=${deltaSym}&resolution=15m&start=${startTs}&end=${nowTs}`,
      { headers: { 'User-Agent': 'projectzero/1.0' } })
    const rawData = await r.json()
    const klines = rawData.result || []
    if (klines.length < 30)
      return res.status(500).json({error:`Not enough Delta data: ${klines.length} candles`,signal:'HOLD',confidence:0})

    const closes  = klines.map(k => parseFloat(k.close))
    const highs   = klines.map(k => parseFloat(k.high))
    const lows    = klines.map(k => parseFloat(k.low))
    const volumes = klines.map(k => parseFloat(k.volume))